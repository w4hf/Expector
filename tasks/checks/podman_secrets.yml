---
# check type: podman_secrets
# Spec: secrets[], expected_state (present|absent)
# Optional: podman_user, become (default false — rootless podman)
# Lists names with: podman secret ls --format '{{.Name}}'
# Remediate: not applicable

- name: Podman secrets | Initialize
  ansible.builtin.set_fact:
    _podman_become: "{{ current_check.become | default(_podman_user | length > 0) | bool }}"
    _podman_env: {}
  vars:
    _podman_user: "{{ current_check.podman_user | default('') | string | trim }}"

- name: Podman secrets | Verify
  block:
    - name: Podman secrets | Verify | Require a secrets list
      when: current_check.secrets is not defined or current_check.secrets | length == 0
      ansible.builtin.set_fact:
        _spec_invalid: true
        check_success: false
        check_msg: "Check '{{ current_check.name | default('podman_secrets') }}' is missing required field 'secrets'"
        check_error: "Required field 'secrets' is missing or empty"

    - name: Podman secrets | Verify | Resolve podman user runtime
      when:
        - current_check.podman_user | default('') | string | trim | length > 0
        - not (_spec_invalid | bool)
      block:
        - name: Podman secrets | Verify | Get podman user UID
          ansible.builtin.command:
            argv: [id, -u]
          register: _podman_uid
          changed_when: false
          become: true
          become_user: "{{ current_check.podman_user }}"

        - name: Podman secrets | Verify | Set rootless podman environment
          ansible.builtin.set_fact:
            _podman_env:
              XDG_RUNTIME_DIR: "/run/user/{{ _podman_uid.stdout | trim }}"
              DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ _podman_uid.stdout | trim }}/bus"

    - name: Podman secrets | Verify | List secret names
      when: not (_spec_invalid | bool)
      ansible.builtin.command:
        argv:
          - podman
          - secret
          - ls
          - --format
          - "{% raw %}{{.Name}}{% endraw %}"
      register: _podman_secret_ls
      failed_when: false
      changed_when: false
      become: "{{ _podman_become }}"
      become_user: "{{ current_check.podman_user if (current_check.podman_user | default('') | string | trim | length > 0) else omit }}"
      environment: "{{ _podman_env }}"

    - name: Podman secrets | Verify | Handle list failure
      when:
        - not (_spec_invalid | bool)
        - _podman_secret_ls.rc | default(1) | int != 0
      ansible.builtin.set_fact:
        _spec_invalid: true
        check_success: false
        check_msg: "Failed to list podman secrets"
        check_error: "{{ _podman_secret_ls.stderr | default(_podman_secret_ls.msg | default('podman secret ls failed')) | trim }}"

    - name: Podman secrets | Verify | Parse secret names
      when: not (_spec_invalid | bool)
      ansible.builtin.set_fact:
        _podman_secret_names: "{{ (_podman_secret_ls.stdout | default('')).splitlines() | map('trim') | reject('equalto', '') | reject('equalto', 'NAME') | list }}"

    - name: Podman secrets | Verify | Fail clearly when podman returned no secrets
      when:
        - not (_spec_invalid | bool)
        - (_podman_secret_names | default([]) | length) == 0
        - (current_check.expected_state | default('present')) == 'present'
      ansible.builtin.set_fact:
        _spec_invalid: true
        check_success: false
        check_msg: "podman secret ls returned no secrets"
        check_error: "No secrets were listed. The check runs as the connecting user by default; set become: true for rootful podman, or podman_user: <name> for another user's rootless containers."

    - name: Podman secrets | Verify | Evaluate each secret
      when: not (_spec_invalid | bool)
      ansible.builtin.set_fact:
        _check_details: "{{ _check_details + [_sec_result] }}"
      loop: "{{ current_check.secrets | default([]) }}"
      loop_control:
        label: "{{ item | trim }}"
      vars:
        _name: "{{ item | trim }}"
        _expected_state: "{{ current_check.expected_state | default('present') }}"
        _exists: "{{ _name in (_podman_secret_names | default([])) }}"
        _ok: "{{ (_expected_state == 'present' and (_exists | bool)) or (_expected_state == 'absent' and not (_exists | bool)) }}"
        _actual_state: "{{ 'present' if (_exists | bool) else 'absent' }}"
        _sec_result:
          name: "{{ _name }}"
          expected_state: "{{ _expected_state }}"
          actual_state: "{{ _actual_state }}"
          result: "{{ 'pass' if (_ok | bool) else 'fail' }}"
          error: "{{ '' if (_ok | bool) else (_name ~ ': ' ~ _actual_state ~ ' (expected ' ~ _expected_state ~ ')') }}"

    - name: Podman secrets | Verify | Set check status
      when: not (_spec_invalid | bool)
      ansible.builtin.set_fact:
        check_success: "{{ _sec_failures | length == 0 }}"
        check_msg: "{{ 'All ' ~ (_check_details | length | string) ~ ' secrets match expected state' if (_sec_failures | length == 0) else ((_sec_failures | length | string) ~ ' of ' ~ (_check_details | length | string) ~ ' secrets are not in the expected state') }}"
        check_error: "{{ _sec_failures | map(attribute='error') | reject('equalto', '') | list | join('; ') }}"
      vars:
        _sec_failures: "{{ _check_details | selectattr('result', 'equalto', 'fail') | list }}"

  rescue:
    - name: Podman secrets | Verify | Capture unexpected error
      ansible.builtin.set_fact:
        check_success: false
        check_msg: "Podman secret verification raised an unexpected error"
        check_error: "{{ ansible_failed_result.msg | default(ansible_failed_result.stderr | default(ansible_failed_result | string)) }}"

- name: Podman secrets | Remediate
  when: _do_remediate | bool
  ansible.builtin.set_fact:
    _remediate_attempted: false
    _remediate_success: false
    _remediate_msg: "No remediation is available for check type 'podman_secrets'"
    _remediate_error: ""

- name: Podman secrets | Report
  ansible.builtin.include_tasks:
    file: "{{ playbook_dir }}/tasks/checks/_report.yml"
